# Legacy 参考代码

本目录仅保留尚未整合到统一 pipeline 的早期实验快照，不是当前运行入口。

- `avatar_compression_legacy.py`：早期在 AvatarNet 内部直接改写 Posemap/压缩逻辑的参考版。

当前实现以 `network/avatar.py`、`utils/pose_map_io.py` 和 `hgc_avatar/pipeline.py` 为准。本目录文件不参与正常运行或测试。
