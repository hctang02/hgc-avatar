# 服务器目录清理记录（2026-08-13）

原目录：`/mnt/ssd2tB/haocheng/AnimatableGaussians`

清理原则：保留原始数据、授权人体模型、训练结果、正式压缩源检查点、codec 权重和参考文档；只删除可由当前代码重新生成的历史中间结果、缓存、重复副本和失效链接。

已删除：

- `compress_part/data-old/`（重复旧数据副本，约 49GB）；
- `test_results/`、`test_results---ACMMM/`（旧测试渲染，合计约 13GB）；
- `compress_part/data/{lbn1,zzr,thu00,thu02}_point*_q=*`（16 个旧压缩点，约 21GB）；
- `debug/`、`tmp_quant/`、`output-test*`、旧训练/测试 output 目录；
- `pose_map_aftercompress*`、`codec_part_file/`、`bins/`、`__pycache__/`；
- 根目录 `log.txt`、`train.log` 和失效的 `results---ACMMM` 符号链接。

参考文档移动到 `docs/references/`：

- `ACMMM_2025_CamreaReady.pdf`；
- HGC-Avatar 运行指南 PDF/PPTX；
- N4120 三维体视频智能编码标准 DOCX。

清理后 `ssd2tB` 可用空间约 87GB（清理前不足 1GB）。删除内容不在 Git 中，不能通过 Git 恢复，但均可由保留的数据、检查点和当前 pipeline 重新生成。
